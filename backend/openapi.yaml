# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Chat API
    description: The chat service definition.
    version: 0.0.1
paths:
    /chat/msg:
        post:
            tags:
                - Chat
            description: Send a chat message
            operationId: Chat_SendMessage
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SendMessageRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/SendMessageReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /chat/send:
        post:
            tags:
                - Chat
            description: Stream chat messages for real-time communication
            operationId: Chat_StreamMessages
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/StreamMessagesRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/StreamMessagesReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/chat/messages:
        get:
            tags:
                - Chat
            description: Get chat messages
            operationId: Chat_GetMessages
            parameters:
                - name: sessionId
                  in: query
                  schema:
                    type: string
                - name: page
                  in: query
                  schema:
                    type: integer
                    format: int32
                - name: pageSize
                  in: query
                  schema:
                    type: integer
                    format: int32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetMessagesReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        ChatMessage:
            type: object
            properties:
                id:
                    type: string
                content:
                    type: string
                userId:
                    type: string
                sessionId:
                    type: string
                timestamp:
                    type: string
                type:
                    type: string
            description: Chat message structure
        GetMessagesReply:
            type: object
            properties:
                messages:
                    type: array
                    items:
                        $ref: '#/components/schemas/ChatMessage'
                total:
                    type: integer
                    format: int32
            description: Reply for getting messages
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        Message:
            type: object
            properties:
                role:
                    type: string
                    description: 消息发送者角色 ("system", "user", "assistant")
                content:
                    type: string
                    description: 消息内容
            description: Message structure for chat history
        SendMessageReply:
            type: object
            properties:
                messageId:
                    type: string
                success:
                    type: boolean
                errorMessage:
                    type: string
            description: Reply for sending a message
        SendMessageRequest:
            type: object
            properties:
                maxTokens:
                    type: integer
                    description: |-
                        // 用户输入的内容
                         string content = 1;
                         // 用户唯一标识符
                         string user_id = 2;
                         // 会话唯一标识符
                         string session_id = 3;
                         最大生成令牌数
                    format: int32
                model:
                    type: string
                    description: 使用的AI模型名称
                temperature:
                    type: number
                    description: 控制生成文本随机性的参数，值越高结果越随机
                    format: float
                topP:
                    type: number
                    description: 控制生成文本多样性的参数，0-1之间
                    format: float
                presencePenalty:
                    type: number
                    description: 存在惩罚因子，防止重复话题
                    format: float
                frequencyPenalty:
                    type: number
                    description: 频率惩罚因子，防止重复词汇
                    format: float
                messages:
                    type: array
                    items:
                        $ref: '#/components/schemas/Message'
                    description: 聊天历史记录消息列表
                stream:
                    type: boolean
                    description: 是否启用流式响应
                kid:
                    type: string
                    description: 密钥标识符
                appId:
                    type: string
                    description: 应用程序标识符
                hasAttachment:
                    type: boolean
                    description: 是否包含附件
                autoSelectModel:
                    type: boolean
                    description: 是否自动选择模型
            description: Request for sending a message
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        StreamMessagesReply:
            type: object
            properties:
                message:
                    $ref: '#/components/schemas/ChatMessage'
            description: Reply for streaming messages
        StreamMessagesRequest:
            type: object
            properties:
                sessionId:
                    type: string
            description: Request for streaming messages
tags:
    - name: Chat
